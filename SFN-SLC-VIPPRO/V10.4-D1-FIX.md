# V10.4 Pages D1 installer fix

- `PRAGMA foreign_keys` has been physically removed from runtime schema strings and SQL fallbacks.
- `/api/health` reports `V10.4 Pages D1 Fix` so you can verify the new deployment is actually active.
- The installer remains retry-safe for partially created schemas.
