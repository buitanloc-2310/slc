{
  "name": "sfn-slc",
  "version": "12.0.0-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name slc",
    "db:migrate": "wrangler d1 migrations apply sfn-slc-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply sfn-slc-db --local",
    "check": "node --check src/index.js && node --check src/schema-v11.js && node --check src/live-room.js && node --check public/app.js && node --check functions/api/[[path]].js"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
